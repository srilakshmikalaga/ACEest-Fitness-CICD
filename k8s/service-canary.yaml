apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-service-canary
  labels:
    app: aceest-fitness
    version: canary
spec:
  selector:
    app: aceest-fitness
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
